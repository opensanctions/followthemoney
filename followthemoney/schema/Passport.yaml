Passport:
  label: "Passport"
  plural: "Passports"
  description: >
    An passport held by a person.
  extends:
    - Identification
  matchable: false
  featured:
    - number
    - country
    - type
    - holder
    - startDate
    - endDate
  required:
    - holder
    - number
  caption:
    - passportNumber
    - number
  properties:
    passportNumber:
      # FIXME: Use `number` from `Identification` instead!!
      label: "Passport number"
      type: identifier
      hidden: true
    surname:
      # FIXME: wtf. make this `lastName` ???
      label: "Surname"
      deprecated: true
    givenName:
      # FIXME: firstName
      label: "Given name"
    # TODO: add middleName
    birthDate:
      label: "Birth date"
      type: date
    birthPlace:
      label: "Place of birth"
    gender:
      label: "Gender"
      type: gender
    personalNumber:
      label: "Personal number"
      type: identifier
